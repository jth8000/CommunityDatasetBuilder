Dataset(DatabaseId(primary), DatasetName)

Feature(FeatureId(primary), DatasetId(foreign), Feature_Name)
*DatasetId references Dataset

InputData(DataGroupId, FeatureId(foreign), Value)
*FeatureId references Feature

Label(DataGroupId, LabelData, UpVotes)
